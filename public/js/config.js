const CONFIG = {
  API_URL: '/api',
  SOCKET_URL: window.location.origin,
  PREVIEW_DURATION: 30000,
  MAX_PLAYERS: 4,
  MIN_PLAYERS: 2,
  DEFAULT_ROUNDS: 10,
  MAX_MESSAGE_LENGTH: 200
};

export default CONFIG;
